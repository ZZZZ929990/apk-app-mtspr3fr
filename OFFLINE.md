# האפליקציה שלי — גרסה עצמאית (בלי אינטרנט ובלי גוגל)

זו גרסה נפרדת לגמרי מהגרסה שמציגה את האתר.

## מה מיוחד בה
- כל התוכן נמצא **בתוך** קובץ ה-APK (`app/src/main/assets/www`).
- אין הרשאת INTERNET במניפסט — האפליקציה פשוט לא יכולה לגשת לרשת.
- אין תלות בשירותי Google Play, אין חשבון גוגל, אין חנות אפליקציות.
- המסך הראשי הוא תפריט עם הכפתורים והמשחקים שהגדרת.

## איך מקבלים APK
בענן (בלי להתקין כלום): העלה את התיקייה ל-GitHub ← לשונית Actions ← Build APK ← Run workflow ← הורד מ-Artifacts.
במחשב: `gradle assembleDebug` והקובץ יהיה ב-`app/build/outputs/apk/debug/app-debug.apk`.

## התקנה בטלפון
העבר את ה-APK לטלפון, פתח אותו ואשר "התקנה ממקורות לא ידועים". אחרי ההתקנה אפשר לנתק את האינטרנט לגמרי והאפליקציה תמשיך לעבוד.

## עריכת התוכן בלי לבנות מחדש
כל הקבצים ב-`app/src/main/assets/www` הם HTML רגיל. אפשר לפתוח אותם גם ישירות בדפדפן במחשב או בטלפון.
